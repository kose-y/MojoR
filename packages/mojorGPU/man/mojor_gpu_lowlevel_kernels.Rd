% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roxygen_gpu_docs.R
\name{mojor_gpu_lowlevel_kernels}
\alias{mojor_gpu_lowlevel_kernels}
\alias{mojor_sigmoid_f32_gpu}
\alias{mojor_sigmoid_f64_gpu}
\alias{mojor_sigmoid_f32_gpu_iters}
\alias{mojor_sigmoid_affine_f32_gpu}
\alias{mojor_sigmoid_affine_f32_gpu_iters}
\alias{mojor_sigmoid_affine_f32_gpu_chain}
\alias{mojor_sigmoid_affine_f32_gpu_chain_sum}
\alias{mojor_gpu_chain}
\title{Low-Level GPU Kernel Wrappers}
\usage{
mojor_sigmoid_f32_gpu(x)

mojor_sigmoid_f64_gpu(x)

mojor_sigmoid_f32_gpu_iters(x, iters = 5L)

mojor_sigmoid_affine_f32_gpu(x, scale = 1, bias = 0)

mojor_sigmoid_affine_f32_gpu_iters(x, iters = 5L, scale = 1, bias = 0)

mojor_sigmoid_affine_f32_gpu_chain(
  x,
  iters = 5L,
  scale = 1,
  bias = 0,
  post_scale = 1,
  post_bias = 0,
  post_iters = 0L
)

mojor_sigmoid_affine_f32_gpu_chain_sum(
  x,
  iters = 5L,
  scale = 1,
  bias = 0,
  post_scale = 1,
  post_bias = 0,
  post_iters = 0L,
  reduction = c("block", "warp")
)

mojor_gpu_chain(
  x,
  iters = 5L,
  op = "sigmoid",
  scale = 1,
  bias = 0,
  post_scale = 1,
  post_bias = 0,
  post_iters = 0L
)
}
\arguments{
\item{x}{Input vector (numeric or float32-compatible where applicable).}

\item{iters}{Iteration count for repeated transform application.}

\item{scale}{Affine scale parameter.}

\item{bias}{Affine bias parameter.}

\item{post_scale}{Second-stage affine scale for chained variants.}

\item{post_bias}{Second-stage affine bias for chained variants.}

\item{post_iters}{Second-stage iteration count for chained variants.}

\item{reduction}{Reduction backend used by \code{mojor_sigmoid_affine_f32_gpu_chain_sum()}.}

\item{op}{Operation family for \code{mojor_gpu_chain()} (currently \code{"sigmoid"}).}
}
\value{
\itemize{
\item f32 kernels return float32 vectors (class from package \code{float}).
\item \code{mojor_sigmoid_f64_gpu()} returns a numeric vector.
\item \code{mojor_sigmoid_affine_f32_gpu_chain_sum()} returns a numeric scalar sum.
}
}
\description{
Direct wrappers around MöjoR GPU kernels for sigmoid/affine transformation pipelines.
}
\examples{
\dontrun{
x <- runif(1024)
y <- mojor_sigmoid_affine_f32_gpu_chain(x, iters = 3, scale = 1.2, bias = 0.1)
s <- mojor_sigmoid_affine_f32_gpu_chain_sum(x, reduction = "block")
}
}
