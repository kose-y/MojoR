% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roxygen_gpu_docs.R
\name{mojor_gpu_session}
\alias{mojor_gpu_session}
\alias{mojor_gpu_session_run}
\alias{mojor_gpu_session_sum}
\alias{mojor_gpu_session_free}
\title{GPU Session API}
\usage{
mojor_gpu_session(x)

mojor_gpu_session_run(
  session,
  iters = 5L,
  scale = 1,
  bias = 0,
  post_scale = 1,
  post_bias = 0,
  post_iters = 0L
)

mojor_gpu_session_sum(
  session,
  iters = 5L,
  scale = 1,
  bias = 0,
  post_scale = 1,
  post_bias = 0,
  post_iters = 0L
)

mojor_gpu_session_free(session)

show(object)
}
\arguments{
\item{session}{A \code{mojor_gpu_session} object.}
}
\value{
\itemize{
\item \code{mojor_gpu_session()} and \code{mojor_gpu_session_run()} return formal S4 \code{mojor_gpu_session} objects.
\item \code{mojor_gpu_session_sum()} returns a numeric scalar.
\item \code{mojor_gpu_session_free()} returns the session object invisibly after release.
}
}
\description{
Stateful GPU session helpers for repeated chained operations over float32 payloads.
}
\examples{
\dontrun{
s <- mojor_gpu_session(runif(1024))
s <- mojor_gpu_session_run(s, iters = 3)
mojor_gpu_session_sum(s)
mojor_gpu_session_free(s)
}
}
