% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roxygen_api_docs.R
\name{mojor_fn}
\alias{mojor_fn}
\title{Function-First Convenience API for Building Kernels}
\usage{
mojor_fn(signature, body, ..., name = NULL, cache = TRUE, load = TRUE, env = parent.frame())
}
\arguments{
\item{signature}{Either an R function or a signature string such as \code{"sum1(x: f64[]) -> f64"}.}

\item{body}{Body expression/string when \code{signature} is a signature string.}

\item{...}{Named type annotations passed through to \code{\link[=mojor_build]{mojor_build()}} for function signatures.}

\item{name}{Optional kernel symbol name override.}

\item{cache}{If \code{TRUE}, reuse build cache entries where possible.}

\item{load}{If \code{TRUE}, return a callable compiled function; otherwise return build metadata.}

\item{env}{Environment used when parsing signature-string bodies.}
}
\value{
If \code{load = TRUE}, returns a callable R function. Otherwise returns the list
produced by \code{\link[=mojor_build]{mojor_build()}}.
}
\description{
Convenience wrapper around \code{\link[=mojor_build]{mojor_build()}} for function-centric and signature-string workflows.
}
\details{
When \code{signature} is an R function, all formal arguments must receive explicit type
hints via \code{...}.
}
\examples{
f <- function(x) {
y <- numeric(length(x))
for (i in 1:length(x)) y[i] <- x[i] + 1
y
}

\dontrun{
g <- mojor_fn(f, x = "f64[]")
g(c(1, 2, 3))
}
}
