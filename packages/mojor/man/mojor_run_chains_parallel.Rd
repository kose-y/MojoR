% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roxygen_api_docs.R
\name{mojor_run_chains_parallel}
\alias{mojor_run_chains_parallel}
\title{Run Independent Chains in Parallel Workers}
\usage{
mojor_run_chains_parallel(
chain_fun,
n_chains,
chain_args = list(),
seed = 1L,
workers = getOption("mc.cores", 2L),
backend = c("auto", "fork", "sequential"),
use_mojor_rng = TRUE
)
}
\arguments{
\item{chain_fun}{Function that runs one chain.}

\item{n_chains}{Positive integer number of chains.}

\item{chain_args}{Named list of arguments passed to \code{chain_fun}.}

\item{seed}{Base integer seed. Chain \code{i} uses \code{seed + i - 1}.}

\item{workers}{Positive integer worker count hint.}

\item{backend}{One of \code{"auto"}, \code{"fork"}, or \code{"sequential"}.}

\item{use_mojor_rng}{If \code{TRUE}, seed with \code{mojor_rng_seed()}; otherwise use \code{set.seed()}.}
}
\value{
List of per-chain outputs, ordered by chain id.
}
\description{
Execute independent chains with deterministic per-chain seeding and optional fork-based
parallelism.
}
\examples{
chain_fun <- function(n) {
x <- numeric(n)
for (i in seq_len(n)) x[i] <- i
sum(x)
}

mojor_run_chains_parallel(
chain_fun = chain_fun,
n_chains = 2,
chain_args = list(n = 5),
backend = "sequential",
use_mojor_rng = FALSE
)
}
